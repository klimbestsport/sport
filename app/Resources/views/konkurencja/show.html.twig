{% extends 'base.html.twig' %}

{% block body %}
    <h1>Konkurencja</h1>

    <table>
        <tbody>
            <tr>
                <th>Nazwas</th>
                <td>{{ konkurencja.nazwaS }}</td>
            </tr>
            <tr>
                <th>Nazwap</th>
                <td>{{ konkurencja.nazwaP }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ konkurencja.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('konkurencja_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('konkurencja_edit', { 'id': konkurencja.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
